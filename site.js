const toggle=document.querySelector('.menu-toggle');
const menu=document.getElementById('mobile-nav');
if(toggle&&menu){toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));toggle.setAttribute('aria-label',open?'Open navigation':'Close navigation');menu.hidden=open;toggle.innerHTML=open?'Menu <span>+</span>':'Close <span>−</span>';});document.addEventListener('keydown',event=>{if(event.key==='Escape'&&!menu.hidden){toggle.click();toggle.focus();}});}
const form=document.getElementById('enquiry');
if(form){const query=new URLSearchParams(location.search).get('service');if(query&&Array.from(form.elements.service.options).some(option=>option.value===query))form.elements.service.value=query;form.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const data=new FormData(form);const service=form.elements.service.selectedOptions[0].textContent;const subject='HealOra project enquiry'+(data.get('service')?' — '+service:'');const body=`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nOrganisation: ${data.get('company')}\nService: ${data.get('service')?service:'To discuss'}\n\n${data.get('message')}`;location.href='mailto:healora98@gmail.com?subject='+encodeURIComponent(subject)+'&body='+encodeURIComponent(body);document.getElementById('form-status').textContent='Your email draft is ready to open. If your email app does not open, email your brief directly to healora98@gmail.com. Nothing has been sent by this page.';});}
